function line_points = get_line_points(vector_hor, vector_ver)
    % this cannot be used by my real implementation, as 1/8 notes can also
    % form a straight line to both ends of the image strip
    line_points = [7,11,15,19,23];
end
