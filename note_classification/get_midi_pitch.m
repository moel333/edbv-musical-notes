function midi_pitch = get_midi_pitch(upper_loc, lower_loc, line_diff, c_loc)
%   c value is encoded as 60, we use it as default
%   it is located on the first lower nonexistant staff line of G clef
%   notes
%   one inc/dec means half higher/lower pitch
    
end
